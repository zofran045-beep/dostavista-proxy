{
  "name": "dostavista-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for Dostavista API",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}